library(datasets)

head(iris)
?plot
plot(iris$Species) # Categorial plot
plot(iris$Petal.Length) # Quantitative variabil
plot(iris$Petal.Species, iris$) # Cat x quant
plot(irix$Petal.Length, iris$Petal.Width) # Quant pair
plot(iris); #Entire data frame


